# 参考项目：外部克隆的仓库，不属于本项目源码
reference_project/

# Claude Code 本机配置（每台机器不同，不入库）
.claude/settings.local.json

# .claude/skills 是指向 .agents/skills 的 NTFS junction（本机链接，不入库；
# 真实的 skills 存储在 .agents/skills/，随仓库提交）
.claude/skills

# Node 工程产物
node_modules/
dist/
coverage/
*.tsbuildinfo

# 检视运行审计痕迹（runReview 默认落盘目录）
runs/

# /implement-spec 子代理 worktree 临时目录
.claude/worktrees/
